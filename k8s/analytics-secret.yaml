apiVersion: v1
kind: Secret
metadata:
  name: analytics-secret
  namespace: pet-api
type: Opaque
stringData:
  # Never commit real values — inject via CI/CD or external-secrets-operator.
  # kubectl create secret generic analytics-secret \
  #   --from-literal=DB_USERNAME=analytics \
  #   --from-literal=DB_PASSWORD=<strong-password> \
  #   --from-literal=ANALYTICS_API_KEY=$(openssl rand -hex 24) \
  #   -n pet-api
  DB_USERNAME: "REPLACE_ME"
  DB_PASSWORD: "REPLACE_ME"
  ANALYTICS_API_KEY: "REPLACE_ME"
